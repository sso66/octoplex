// TimeDateAlerts.js
console.log( "Mounting TimeDateAlerts.js..." );

import { alertClasses } from "./AlertModuleDefs";

const TimeDateAlerts = [
    
    Time = {
        objectId: "AA-01",
        objectTypes: "TimeDate",
        desc: "Enables the setting of an alarm clock in any time zone.",
        alertClasses: [ CLASS_TIMER ]
    }
];

export default TimeDateAlerts;

// eof

// SMSTextAlerts.js
console.log( "Mounting SMSTextAlerts.js..." );

import { alertClasses } from "./AlertModuleDefs";

const SMSTextAlerts = [
    
    SMSStatus = {
        objectId: "AA-01",
        objectTypes: "SMSTextAlerts",
        desc: "Monitors the status of the SMS100 module.",
        alertClasses: [ CLASS_DATA_UNAVAILABLE ],
        instances: null,
        units: ""
    }
];

export default RudderAlerts;

// eof
